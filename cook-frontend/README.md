# CookSync Frontend

Aplicación web para la plataforma CookSync, construida con React.

## 🛠️ Tecnologías

- **React**: Librería de UI.
- **React Router**: Navegación.
- **Axios**: Cliente HTTP.
- **CSS Modules**: Estilizado.

## 🚀 Configuración e Inicio

### 1. Instalar dependencias

```bash
npm install
```

### 2. Iniciar Aplicación

```bash
npm start
```

La aplicación se abrirá automáticamente en `http://localhost:3000`.

## 📦 Estructura de Carpetas

- `src/`: Código fuente.
  - `components/`: Componentes reutilizables.
  - `pages/`: Vistas principales de la aplicación.
  - `services/`: Comunicación con el backend.
  - `context/`: Gestión de estado global (Auth, Notificaciones).

## 🔗 Conexión con Backend

El frontend espera que el backend esté corriendo en `http://localhost:3002`. Si necesitas cambiar esto, revisa los archivos en `src/services/`.

---
© 2025 CookSync Team
